import { SectionPlaceholder } from "@/components/layout/section-placeholder";

export default function BuildTasksPage() {
  return (
    <SectionPlaceholder
      title="Сборка / Задачи"
      description="Экран build/tasks перенесен в React каркас и ожидает подключение бизнес-логики API."
    />
  );
}
