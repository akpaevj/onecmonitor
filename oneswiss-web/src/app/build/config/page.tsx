import { SectionPlaceholder } from "@/components/layout/section-placeholder";

export default function BuildConfigPage() {
  return (
    <SectionPlaceholder
      title="Сборка / Настройки"
      description="Экран build/config перенесен в React каркас и ожидает подключение бизнес-логики API."
    />
  );
}
