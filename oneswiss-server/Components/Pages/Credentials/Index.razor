@page "/credentials"
@using OneSwiss.Server.Components.Shared
@using OneSwiss.Server.Models
@rendermode InteractiveServer

<DatabaseObjectsGrid T="Credentials" NameSelector="arg => arg.Name" Route="credentials" Title="Учетные данные">
    <ColumnsContent>
        <PropertyColumn Property="@(arg => arg.Name)"/>
        <PropertyColumn Property="@(arg => arg.DefaultForClusters)"/>
        <PropertyColumn Property="@(arg => arg.DefaultV8Admin)"/>
    </ColumnsContent>
</DatabaseObjectsGrid>